# `10` Print a random excuse

## 📝 Instructions:

1. Create a new variable named `excuse` which value should be the concatenation of the values generated in the previous step.

2. Print the variable `excuse` in the console.